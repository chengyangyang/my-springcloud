

spring:
  application:
    name: service-eureka

server.port: 8901

eureka:
  client:
    fetch-registry: false # 设置为true时,可以启动,但报错
    register-with-eureka: false # 不作为客户端注册到注册中心
   #service-url:
   #  defaultzone: http://localhost:8000/eureka/        #进行高可用设置时，将register-with-eureka 设置为true，默认，更改端口即可
   server:
